from setuptools import setup, find_packages

setup(
    name='doorpost_pose_detector',
    version='0.1.0',
    packages=find_packages(include=['doorpost_pose_detector', 'doorpost_pose_detector.*'])
)
